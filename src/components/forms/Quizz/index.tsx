import { InputText } from 'components/UI'
import React, { FC } from 'react'

export const QuizzForm: FC = () => {
  return (
    <form action="">
      <InputText />
      <InputText />
    </form>
  )
}
